# Execution envelope <job>/<attempt>

- Project / coordinator / sender / recipient: <stable identities>
- Semantic assignment: <accessible path or URL, revision and SHA-256 digest>
- Authority and startup gate: <reference to assignment clause; no additional authorization>
- Selected profile: <client/provider/model/effort; requested versus observed>
- Session/worktree: <owned location, branch, session reuse and machine mapping>
- Allowed tools/resources and exact launch: <resolved route and limits; no secrets>
- Supervisor / deadline / output budget: <bounded lifecycle>
- Return route / receiver / correlation: <supported route, attempt and event identity>
- Durable question file / handover / evidence root: <paths>
- Previous attempt: <if resuming; preserve its evidence>

Attach or resolve the semantic assignment unchanged before execution. It owns what to build, acceptance, scope, required checks and delivery authority. This envelope owns how that revision is executed; unresolved semantic fields return to its author. Follow the exchange contract for startup and returns. A composed dispatch may include both layers, but each requirement has one owning record.
